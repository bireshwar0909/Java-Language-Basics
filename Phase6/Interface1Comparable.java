package Basics.Phase6;

public class Interface1Comparable {

    //This interface imposes a total ordering on the objects of each class that implements it. 
    //The ordering is referred to as the class's natural ordering, and the class's compareTo methid is referred to as its
    //          natural comparison method.
    //lists (and Arrays) of objects that implements this interface can be sorted automatically by Collections.sort (and Array.sort).
    //Objects that implements this interface can be used as keys in a sorted map or as elements in a sorted set, without 
    //         the need to specify the need of an comparator.
    
}
