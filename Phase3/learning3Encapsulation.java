package Basics.Phase3;

public class learning3Encapsulation {
    public static void main(String[] args){
        
        //private : the access level of a private modifier is only within the class. it cannot be accessed from outside the class
        //defalut : the access level of a default modifier is only within the package. it cannot be accessed from outside the package.
        //              if you do not specify any access level, it will be default.
        //protected : the access level of a protected modifier is within the package and outside the package through child class. 
        //            if you do not make the child class, it cannot be accessed from outside the package
        //public : the access level of a public modifier is everywhere. it can be accessed from within the classs, outside the class, within
        //          within the package and outside the package.
        
    }
    
}
